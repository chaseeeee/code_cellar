'use strict';

consile.log ('before the if statement');

var numberOfTimesVoted = 1;

if (false) {
    console.log(body of the if statesment);

} else if () {
    console.log (' condition if evaluated to be false');
    console.log ('else branch');
}

console.log ('after the if statement');
